所有更新数据库的SQL文都在game_db的sql目录下
db_init.sql为基础SQL文，包含最初的建表语句，此文件名不可变
后续添加的SQL文必须以数字为文件名，“.sql为后缀”，建议以日期为文件名，比如：20140723.sql。
如果今天是2014年7月23日，但此日子的SQL文已经被使用了，那么请你继续使用下一天的日子“20140724”。更新的SQL文不会太多，所以日子总体是够用的。
SQL文的执行顺序就是数字的从小到大的顺序