_G.netManager.registeDecoder(_G.messageType.$messagetype, "$decodemessage")
--请复制下面注释到合适的位置，在函数内编写相关的业务逻辑即可
--local function ${messagetype}Handler(message)
--${tab}local $vars = $args
--end
--localGlobal.netManager.registerMessageHandler(localGlobal.messageType.$messagetype, ${messagetype}Handler)
